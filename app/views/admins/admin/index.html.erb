<div class="jumbotron jumbotron-fluid rounded" style="margin-top: 5vh">
  <div class="container text-center">
    <h1 class="montserrat-light">Welcome on your admin dashboard <%= current_user.first_name %></h1>
    <p class="lead lora-regular">You can access your tools below</p>
  </div>
</div>

<div class="row justify-content-center">
	<div class="col-md-6 col-sm-10">
		<div class="list-group">
		  
		  
		  
		</div>

		<ul class="list-group">
		  <li class="list-group-item d-flex justify-content-between align-items-center">
		    <%= link_to "See all the users", admins_users_path, :class => "text-dark" %>
		    <span class="badge badge-primary badge-pill"><%= @users_count %></span>
		  </li>
		  <li class="list-group-item d-flex justify-content-between align-items-center">
		    <%= link_to "See all the events submissions", admins_event_submissions_path, :class => "text-dark" %>
		    <span class="badge badge-primary badge-pill"><%= @sub_events_count %></span>
		  </li>
		  <li class="list-group-item d-flex justify-content-between align-items-center">
		    <%= link_to "See all the validated events", admins_events_path, :class => "text-dark" %>
		    <span class="badge badge-primary badge-pill"><%= @val_events_count %></span>
		  </li>
		</ul>
	</div>
</div>