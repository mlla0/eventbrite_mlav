<div class="row text-center" style="margin-top: 10vh">
<div class="col-12">

<%= form_tag event_attendances_path(params[:event_id]), :method => :post do %>
  <article>
    <% if flash[:error].present? %>
      <div id="error_explanation">
        <p><%= flash[:error] %></p>
      </div>
    <% end %>

    <label class="amount">
      <span class="lora-regular">Amount to pay:</span><br/>
      	<span class="display-1"><%= @event.price %> €</span>
    </label>
  </article>

		  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
		          data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
		          data-description="A month's subscription"
		          data-amount="<%= @stripe_price %>"
		          data-locale="auto">
		  </script>


<% end %>
</div>
</div>